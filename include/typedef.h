#pragma once

#include <vector>
#include <algorithm>

enum Color
{
    RED,
    GREEN,
    BLUE,
    YELLOW,
    RAINBOW
};

enum Direction
{
    UP,
    DOWN,
    LEFT,
    RIGHT
};

enum Shape
{
    CIRCLE,
    SQUARE,
    TRIANGLE,
    STAR
};
