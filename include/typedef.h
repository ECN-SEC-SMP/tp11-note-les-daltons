#pragma once

enum Color{
    RED,
    GREEN,
    BLUE,
    YELLOW,
    RAINBOW
};

enum Direction{
    UP,
    DOWN,
    LEFT,
    RIGHT
};

enum Shape{
    CIRCLE,
    SQUARE,
    TRIANGLE,
    STAR
};